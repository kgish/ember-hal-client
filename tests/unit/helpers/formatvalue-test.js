import {
  formatvalue
} from 'hal-client/helpers/formatvalue';

module('FormatvalueHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatvalue(42);
  ok(result);
});
