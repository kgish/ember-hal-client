import {
  truncate
} from 'hal-client/helpers/truncate';

module('TruncateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = truncate(42);
  ok(result);
});
